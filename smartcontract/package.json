{
  "name": "smartcontract",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deployFactory.ts --network localhost",
    "deploy:sepolia": "hardhat run scripts/deployFactory.ts --network sepolia",
    "deploy:bscTestnet": "hardhat run scripts/deployFactory.ts --network bscTestnet",
    "export:abi": "ts-node scripts/exportAbi.ts",
    "create:escrow": "hardhat run scripts/createEscrow.ts --network localhost"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.2"
  },
  "devDependencies": {
    "@types/node": "^22.8.1",
    "hardhat": "^2.22.12",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
